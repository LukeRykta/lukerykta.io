{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "backend",
  "root": "backend",
  "sourceRoot": "backend/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/gradle.mjs bootRun"
      },
      "cache": false
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/gradle.mjs build",
        "forwardAllArgs": true
      },
      "inputs": [
        "default",
        "^default",
        "gradle"
      ],
      "outputs": [
        "{projectRoot}/build/libs",
        "{projectRoot}/build/reports",
        "{projectRoot}/build/classes",
        "{projectRoot}/build/resources"
      ]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/gradle.mjs test",
        "forwardAllArgs": true
      },
      "inputs": [
        "default",
        "^default",
        "gradle"
      ],
      "outputs": [
        "{projectRoot}/build/test-results",
        "{projectRoot}/build/reports/tests",
        "{projectRoot}/build/reports/jacoco",
        "{projectRoot}/build/jacoco"
      ]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/gradle.mjs clean"
      }
    }
  },
  "tags": []
}
