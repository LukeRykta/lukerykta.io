app:
  frontend:
    url: http://localhost:4200
server:
  port: 8080
logging:
  level:
    root: INFO
    io.lukerykta: DEBUG               # your code package
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG          # optional: show SQL
    org.hibernate.orm.jdbc.bind: TRACE # optional: show SQL params
